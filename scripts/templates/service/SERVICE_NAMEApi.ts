import { createApi, fetchBaseQuery } from "@reduxjs/toolkit/query/react"

export const SERVICE_NAMEApi = createApi({
  reducerPath: "SERVICE_NAMEApi",
  baseQuery: fetchBaseQuery(),
  endpoints: (builder) => ({}),
})

export const {} = SERVICE_NAMEApi
