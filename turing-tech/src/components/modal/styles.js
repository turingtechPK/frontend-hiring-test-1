import styled from "@emotion/styled";
import { Grid } from "@mui/material";

export const DialogContentsContainer = styled(Grid)`
  padding: 20px 20px;
`;

export const DialogContainer = styled(Grid)`
  max-height: 900px;
  overflow-y: auto;
`;
