import { Typography } from "@mui/material"
import { styles } from "./style"

const AddNoteCell = (data) => {
 return (
   <Typography sx={styles.addNoteButton}>Add Note</Typography>
 )
}

export default AddNoteCell