const BASE_URL = "https://frontend-test-api.aircall.io";

export const API_URL ={

    //for login 
    LOGIN: `${BASE_URL}/auth/login`,

    //for getting all calls
    CALLS: `${BASE_URL}/calls`,

    //for refreshing token
    REFRESH_TOKEN: `${BASE_URL}/auth/refresh-token`,
}
